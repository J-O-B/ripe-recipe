"""def messenger():
    try:
        user = session["user"]
        messages = mongo.db.messages.find(
                {"message_for": user})
        return messages
    except:
        error = "Error"
        return error"""
